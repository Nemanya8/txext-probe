import { Binary } from '@polkadot-api/substrate-bindings';
export * from '@polkadot-api/utils';

const jsonSerialize = (_, v) => typeof v === "bigint" ? `${v}n` : v != null && v instanceof Uint8Array ? Binary.toHex(v) : v;
const jsonDeserialize = (_, v) => {
  if (typeof v === "string") {
    if (/^[0-9]+n$/.test(v)) v = BigInt(v.slice(0, -1));
  }
  return v;
};

export { jsonDeserialize, jsonSerialize };
//# sourceMappingURL=utils.js.map
