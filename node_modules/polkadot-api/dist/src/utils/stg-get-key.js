const stgGetKey = (ctx, pallet, name, areAgsCompat) => {
  const getIncompatErr = () => new Error(`Incompatible runtime entry Storage(${pallet}.${name})`);
  let codecs;
  try {
    codecs = ctx.dynamicBuilder.buildStorage(pallet, name);
  } catch {
    throw getIncompatErr();
  }
  return (...args) => {
    if (args.length === codecs.len && !areAgsCompat(args))
      throw getIncompatErr();
    return codecs.keys.enc(...args);
  };
};

export { stgGetKey };
//# sourceMappingURL=stg-get-key.js.map
