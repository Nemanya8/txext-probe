import { createClient } from '../../client.js';
import { middleware } from '@polkadot-api/ws-middleware';
import { getWsProvider as getWsProvider$1 } from '@polkadot-api/ws-provider';
export { SocketEvents, WsEvent } from '@polkadot-api/ws-provider';

const getWsRawProvider = (endpoint, config = {}) => getWsProvider$1(endpoint, config);
const getWsProvider = (endpoint, config = {}) => getWsProvider$1(endpoint, {
  ...config,
  middleware
});
const createWsClient = (endpoint, config = {}) => {
  const provider = getWsProvider(endpoint, config);
  const client = createClient(provider, config);
  return Object.assign(client, {
    switch: provider.switch,
    getStatus: provider.getStatus
  });
};

export { createWsClient, getWsProvider, getWsRawProvider };
//# sourceMappingURL=ws.js.map
