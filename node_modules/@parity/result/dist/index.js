// src/result.ts
function ok(value) {
  return { ok: true, value };
}
function err(error) {
  return { ok: false, error };
}
function unwrapOk(result) {
  if (!result.ok) {
    throw new Error(`unwrapOk: expected ok, got err: ${formatChannel(result.error)}`);
  }
  return result.value;
}
function unwrapErr(result) {
  if (result.ok) {
    throw new Error(`unwrapErr: expected err, got ok: ${formatChannel(result.value)}`);
  }
  return result.error;
}
function normalizeError(cause, ErrorCtor) {
  if (cause instanceof ErrorCtor) return cause;
  const message = cause instanceof Error ? cause.message : String(cause);
  return new ErrorCtor(message, { cause });
}
function isErrorOf(error, ErrorCtor) {
  return error instanceof ErrorCtor;
}
function formatChannel(value) {
  if (value instanceof Error) return value.message;
  if (typeof value === "string") return value;
  try {
    return JSON.stringify(value) ?? String(value);
  } catch {
    return String(value);
  }
}

export { err, isErrorOf, normalizeError, ok, unwrapErr, unwrapOk };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map