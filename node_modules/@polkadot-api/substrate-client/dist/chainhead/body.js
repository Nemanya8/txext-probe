import { chainHead } from '../methods.js';
import { createOperationPromise } from './operation-promise.js';

const createBodyFn = createOperationPromise(
  chainHead.body,
  (hash) => [
    [hash],
    (e, res) => {
      res(e.value);
    }
  ]
);

export { createBodyFn };
//# sourceMappingURL=body.js.map
